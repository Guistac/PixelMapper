{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug PixelMapper (CodeLLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/PixelMapper",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build PixelMapper"
        }
    ]
}
